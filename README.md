![Eslint Logo](assets/banner.png)

# eslint-config

Our preferred code linting rules for [ESLint](https://eslint.org/).

## Usage

```sh
$ npm i -D @psdevs/eslint-config
```

In `package.json`, add:
```json
"eslintConfig": {
  "extends": "@psdevs/eslint-config"
},
```
